name: Lint

on:
  push:
    branches: ['*']
  pull_request:
    branches: ['*']

env:
  NODE: 14.x

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    # lint/action.yml composite
    - name: Lint
      run: yarn lint
