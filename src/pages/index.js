import React from 'react'
import Link from 'gatsby-link'

const Page = () => {
  return (
    <div>
      <Link to="/docs/buttons/" className="btn btn-primary">
        Buttons
      </Link>
    </div>
  );
}

export default Page
